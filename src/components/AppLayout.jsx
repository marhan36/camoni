import { MoreHorizontal, Plus } from 'lucide-react'
import { NAVIGATION } from '../constants/app'
export default function AppLayout({ page, setPage, onAdd, children }) { return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">C</span><span>camoni</span></div><nav>{NAVIGATION.map(([label, Icon]) => <button key={label} className={page === label ? 'nav active' : 'nav'} onClick={() => setPage(label)}><Icon size={19}/><span>{label}</span></button>)}</nav><div className="side-foot"><div className="avatar">M</div><div><b>Marhan</b><small>Pengelola</small></div><MoreHorizontal size={18}/></div></aside><main><header><div><p className="eyebrow">{page === 'Beranda' ? 'Ringkasan keuanganmu' : 'Camoni'}</p><h1>{page}</h1></div><button className="add-button" onClick={onAdd}><Plus size={19}/> Tambah pengeluaran</button></header>{children}</main><div className="mobile-nav">{NAVIGATION.map(([label, Icon]) => <button key={label} className={page === label ? 'active' : ''} onClick={() => setPage(label)}><Icon size={20}/><span>{label}</span></button>)}</div></div> }
