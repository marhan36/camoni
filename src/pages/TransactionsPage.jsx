import { Search } from 'lucide-react'
import TransactionRows from '../components/TransactionRows'
import { findPresetName } from '../models/camoniData'
import { monthLabel } from '../utils/date'
export default function TransactionsPage({ data, month, setMonth, filters, setFilters, onEdit, onDelete }) { const list = data.expenses.filter((expense) => { const person = findPresetName(data.people, expense.personId); const category = findPresetName(data.categories, expense.categoryId); return expense.tanggal.startsWith(month) && (!filters.name || expense.personId === filters.name) && (!filters.category || expense.categoryId === filters.category) && (!filters.search || `${expense.catatan} ${person} ${category}`.toLowerCase().includes(filters.search.toLowerCase())) }).sort((a, b) => b.tanggal.localeCompare(a.tanggal)); return <div className="content"><div className="filter-row"><input type="month" value={month} onChange={(event) => setMonth(event.target.value)}/><div className="search"><Search size={17}/><input placeholder="Cari catatan..." value={filters.search} onChange={(event) => setFilters({ ...filters, search: event.target.value })}/></div><select value={filters.name} onChange={(event) => setFilters({ ...filters, name: event.target.value })}><option value="">Semua nama</option>{data.people.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select value={filters.category} onChange={(event) => setFilters({ ...filters, category: event.target.value })}><option value="">Semua kategori</option>{data.categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div className="panel transaction-panel"><div className="panel-head"><div><h3>Daftar transaksi</h3><p>{list.length} transaksi pada {monthLabel(month)}</p></div></div><TransactionRows data={data} expenses={list} onEdit={onEdit} onDelete={onDelete}/></div></div> }
